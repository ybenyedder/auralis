# Gradle / build output
.gradle/
build/
/local.properties

# IDE
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Signing material — the original key leaked once already; a replacement key
# committed by accident would leak again. CI loads these from secrets only.
app/auralis.keystore
*.keystore
*.jks
keystore.properties
